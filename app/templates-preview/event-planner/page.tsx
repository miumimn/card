import React from "react";
import EventPlannerPreview from "../EventPlannerPreview";

export const metadata = { title: "Event Planner — Template Preview" };

export default function Page() {
  return <EventPlannerPreview />;
}