import React from "react";
import ItSpecialistPreview from "../ItSpecialistPreview";

export const metadata = { title: "IT Specialist — Template Preview" };

export default function Page() {
  return <ItSpecialistPreview />;
}