import React from "react";
import  LawyerPreview from "../LawyerPreview";

export const metadata = { title: "Lawyer — Template Preview" };

export default function Page() {
  return <LawyerPreview />;
}