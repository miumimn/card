import React from "react";
import PhotographerPreview from "../PhotographerPreview";

export const metadata = { title: "Photographer — Template Preview" };

export default function Page() {
  return <PhotographerPreview />;
}