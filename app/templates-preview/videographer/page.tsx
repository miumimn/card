import React from "react";
import VideographerPreview from "../VideographerPreview";

export const metadata = { title: "Videographer — Template Preview" };

export default function Page() {
  return <VideographerPreview />;
}