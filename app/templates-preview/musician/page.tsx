import React from "react";
import MusicianPreview from "../MusicianPreview";

export const metadata = { title: "Musician — Template Preview" };

export default function Page() {
  return <MusicianPreview />;
}