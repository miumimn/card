import React from "react";
import DriverPreview from "../DriverPreview";

export const metadata = { title: "Driver — Template Preview" };

export default function Page() {
  return <DriverPreview />;
}