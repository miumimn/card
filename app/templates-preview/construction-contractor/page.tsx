import React from "react";
import ConstructionPreview from "../constructionPreview";

export const metadata = { title: "Construction / Contractor — Template Preview" };

export default function Page() {
  return <ConstructionPreview />;
}