import React from "react";
import NormalPreview from "../NormalPreview";

export const metadata = { title: "Profile (Full-bleed) — Template Preview" };

export default function Page() {
  return <NormalPreview />;
}