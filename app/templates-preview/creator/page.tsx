import React from "react";
import CreatorPreview from "../CreatorPreview";

export const metadata = { title: "Creator Bundle — Template Preview" };

export default function Page() {
  return <CreatorPreview />;
}