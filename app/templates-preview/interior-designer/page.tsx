import React from "react";
import InteriorDesignerPreview from "../InteriorDesignerPreview";

export const metadata = { title: "Interior Designer — Template Preview" };

export default function Page() {
  return <InteriorDesignerPreview />;
}