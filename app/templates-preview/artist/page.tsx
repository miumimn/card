import React from "react";
import ArtistPreview from "../ArtistPreview";

export const metadata = {
  title: "Artist — Template Preview",
};

export default function Page() {
  return <ArtistPreview />;
}
