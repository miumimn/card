import React from "react";
import GardenerPreview from "../GardenerPreview";

export const metadata = { title: "Gardener — Template Preview" };

export default function Page() {
  return <GardenerPreview />;
}