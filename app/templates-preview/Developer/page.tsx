import React from "react";
import DeveloperPreview from "../DeveloperPreview";

export const metadata = { title: "Developer — Template Preview" };

export default function Page() {
  return <DeveloperPreview />;
}