import React from "react";
import ConsultantPreview from "../consultantPreview";

export const metadata = { title: "Consultant — Template Preview" };

export default function Page() {
  return <ConsultantPreview />;
}